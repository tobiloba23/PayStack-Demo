//This file lists all the modules available to the application with the ng-app directive
//Each module will have controllers that can be call with the ng-controller directive


angular//    module_name,      sub_modules
  .module("paystackClient", [
    "ngMaterial"
  ]);
